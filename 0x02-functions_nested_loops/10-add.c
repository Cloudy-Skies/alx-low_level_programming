#include "main.h"

/**
 * add - a function to add two integers
 *@a: an integer input.
 *@b: an integer input.
 * Returns: an integer
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
